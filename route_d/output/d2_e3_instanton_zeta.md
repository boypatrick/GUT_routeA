# D2-E3 E3-Instanton Zeta Verification

`|zeta| = 0.13043190325293763`.
`arg(zeta) = 0.600038020318215` rad.
`S_eff = -log(|zeta|) = 2.0369040025655094` if the prefactor has unit modulus.
If `zeta = exp(2 pi i T)`, then `Im(T) = 0.3241833406119675` for unit prefactor.
`|zeta|^2 = 0.01701248138618368`.
`|zeta|^2 / Delta_s = 530.1823731560442` for `Delta_s = 3.208798e-05`.

Toy GS charge check: if `q(N)=+1`, then `q(NN)=+2`; an instanton factor of charge `-2` makes the Majorana insertion neutral.  This is only charge bookkeeping, not a global zero-mode proof.

Boundary: this verification does not construct an E3 divisor, does not count charged zero modes, and does not claim a ten-digit `zeta` prediction.
